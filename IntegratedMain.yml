---
    - name: Provision an  Instance
      hosts: 10.136.54.214
#       connection: local
      gather_facts: False
      vars:
         provider: "{{ provider }}"


      
   


      roles:

  
        - { role: azure-vm , when: ('azure' in provider)}
        - { role: aws-ec2 , when: ('aws' in provider)}
        - { role: gce-vm , when: ('gce' in provider)}
        - { role: vcenter-vm , when: ('vc' in provider)}

      # tasks:
    

      #   - import_role:
      #       name: aws-ec2
      #     when: provider == 'aws'
      #     tags: aws

      #   - import_role:
      #       name: azure-vm
      #     when: provider == 'azure'
      #     tags: azure

      #   - import_role:
      #       name: vcenter-vm
      #     when: provider == 'vc'
      #     tags: vcenter

      #   - import_role:
      #       name: gce-vm
      #     when: provider == 'gce'
      #     tags: gce
